*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
local.properties
/evidence/*
!/evidence/.gitkeep
/reports/*
!/reports/.gitkeep
/dist/

# Secrets never belong in this repo. The app needs none today - it has no backend, no account and
# no INTERNET permission - so these are preventive: the only source for a secret is ClaudeSecrets.
.env
.env.*
!.env.example
*.key
*.jks
*.keystore
keystore.properties
signing.properties
