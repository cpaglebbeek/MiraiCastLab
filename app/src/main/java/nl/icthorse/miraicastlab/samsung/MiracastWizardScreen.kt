package nl.icthorse.miraicastlab.samsung

import androidx.compose.foundation.layout.padding
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp
import nl.icthorse.miraicastlab.ui.LabScaffold

/**
 * Miracast test wizard.
 *
 * // STUB - replaced by the implementation agent for this module.
 */
@Composable
fun MiracastWizardScreen(onBack: () -> Unit) {
    LabScaffold(title = "Miracast test wizard", onBack = onBack) {
        Text(
            "Not implemented yet.",
            modifier = Modifier.padding(16.dp),
        )
    }
}
